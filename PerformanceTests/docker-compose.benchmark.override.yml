services:
  auction-worker:
    environment:
      REDIS_URL: redis://redis:6379/1
      EMAIL_DELIVERY_MODE: disabled

  outbox-relay:
    environment:
      REDIS_URL: redis://redis:6379/1
      EMAIL_DELIVERY_MODE: disabled

  async-worker:
    environment:
      REDIS_URL: redis://redis:6379/1
      EMAIL_DELIVERY_MODE: disabled
